# Quickstart: Profile Page

## Running Locally

1. Ensure the database is running.
2. Run migrations: `php artisan migrate`
3. Optional: Seed the database with test users and courses.
4. Access the profile at `/profile`.
5. Access the Filament admin at `/admin` (or configured path) to manage courses and enrollments.
