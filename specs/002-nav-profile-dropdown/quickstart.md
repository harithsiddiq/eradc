# Quickstart

1. Include the `<x-nav.auth-area />` component in the main navigation bar.
2. The `auth-area` component will automatically handle displaying either the guest links or the `<x-nav.profile-dropdown />` component based on authentication state.
